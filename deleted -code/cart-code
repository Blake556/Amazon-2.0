//console.log(itemsInCart)

    // let countItmemsElement = document.querySelectorAll('.number-of-items')

    //     for(let i = 0; i < countItmemsElement.length; i++) {
    //         let bothElements = countItmemsElement[i]

           
    //         let CounterToString = itemsInCart.length.toString()
    //         bothElements.innerHTML = CounterToString 
    //     }
    
        //itemsInCart.addEventListener('load', updateTotal)